---
pageable: true
per_page: 6
---

<div class="articles-column">
  <% if paginate && num_pages > 1 %>
    <p>Página <%= page_number %> de <%= num_pages %></p>

    <% if prev_page %>
      <p><%= link_to 'Anterior página', prev_page %></p>
    <% end %>
  <% end %>
</div>

<div class="articles-column">
  <% page_articles.each_with_index do |article, i| %>
      <article id="preview">
        <h2><%= link_to article.title, article %></h2>
        <h5><%= article.date.strftime('%b %e') %></h5>
        <%= article.summary(200) %>
        <div class='right'>
          <%= link_to 'Leer más...', article , class: 'bottom-read-more'%>
        </div>
      </article>
  <% end %>
</div>

<div class="articles-column">
  <div class='next-page right'>
    <% if paginate %>
      <% if next_page %>
        <p><%= link_to 'Siguiente página', next_page %></p>
      <% end %>
    <% end %>
  </div>
</div>
